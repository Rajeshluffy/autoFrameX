# ============================================================
# Non-sensitive defaults — safe to commit
# Credentials are injected at runtime via environment variables:
#   SERVICE_NOW_USERNAME, SERVICE_NOW_PASSWORD,
#   SERVICE_NOW_CLIENT_ID, SERVICE_NOW_CLIENT_SECRET
# ============================================================

# Browser / UI settings (kept for hybrid UI+API suites)
makaia.browser.name=chrome
makaia.browser.isheadless=false
makaia.implicit.wait.time=15
makaia.explicit.wait.time=30
makaia.test.retry.max.limit=3

# ServiceNow environment — override via -Dservice.now.base.uri=... or env profile
makaia.aut.url=https://dev296642.service-now.com/

# JIRA API Config Details
jira.baseUri=https://karthikeselene.atlassian.net
jira.basePath=/rest/api/3/

# UIBank Config Details
uibank.frontend.url=https://uibank.uipath.com/
uibank.backend.base.uri=https://uibank-api.azurewebsites.net
uibank.backend.base.path=/api/
