<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>autoFrameX</groupId>
	<artifactId>autoframex-parent</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<!--
	    Reactor parent for the TD-20 multi-module split (see
	    docs/TECHNICAL_DEBT_REGISTER.md and docs/ARCHITECTURE.md). A team depends
	    on only the module(s) it needs instead of pulling all ~24 dependencies
	    transitively — e.g. an API-only team takes autoframex-core +
	    autoframex-api, never Selenium/JMeter-adjacent/ZAP-adjacent code.

	    Module dependency order (no cycles): core has zero dependencies on any
	    other module; every other module depends on core (directly or
	    transitively); selenium/api/database sit beside each other; cucumber
	    depends on core+selenium+api; performance depends on core+selenium+api;
	    security depends on core+api; testkit depends on everything (it hosts
	    the one suite — testng.xml — that spans selenium+cucumber+performance+
	    security via <packages>, which no single non-aggregator module's
	    classpath can satisfy).
	-->
	<modules>
		<module>autoframex-core</module>
		<module>autoframex-selenium</module>
		<module>autoframex-api</module>
		<module>autoframex-database</module>
		<module>autoframex-cucumber</module>
		<module>autoframex-performance</module>
		<module>autoframex-security</module>
		<module>autoframex-testkit</module>
	</modules>

	<properties>
		<java.version>17</java.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<testng.suite.file>testng.xml</testng.suite.file>
		<!-- Default: coverage-percentage check WARNs but doesn't fail an
		     otherwise-green build (see the jacoco-check execution below).
		     Override with -Djacoco.haltOnFailure=true to make it a hard gate. -->
		<jacoco.haltOnFailure>false</jacoco.haltOnFailure>
		<browser>chrome</browser>
		<env>qa</env>
		<headless>false</headless>
		<selenium.version>4.33.0</selenium.version>
		<testng.version>7.7.0</testng.version>
		<apache.poi.version>5.2.4</apache.poi.version>
		<extent.report.version>5.1.2</extent.report.version>
		<rest.assured.version>5.5.0</rest.assured.version>
		<gson.version>2.11.0</gson.version>
		<commons.io.version>2.15.1</commons.io.version>
		<cucumber.jvm.version>7.21.1</cucumber.jvm.version>
		<cucumber.jvm.testng.version>7.21.1</cucumber.jvm.testng.version>
		<cucumber.picocontainer.version>7.21.1</cucumber.picocontainer.version>
		<jackson.databind.version>2.18.0</jackson.databind.version>
		<owner.version>1.0.12</owner.version>
		<faker.version>1.0.2</faker.version>
		<mysql.version>9.6.0</mysql.version>
		<snakeyaml.version>2.6</snakeyaml.version>
		<protobuf.version>4.34.1</protobuf.version>
		<jspecify.version>1.0.0</jspecify.version>
		<slf4j.version>2.0.5</slf4j.version>
		<logback.version>1.4.7</logback.version>
		<logstash.encoder.version>7.4</logstash.encoder.version>
		<archunit.version>1.3.0</archunit.version>
		<wiremock.version>3.9.1</wiremock.version>
		<sonar.projectKey>autoFrameX</sonar.projectKey>
		<sonar.projectName>autoFrameX</sonar.projectName>
		<sonar.java.source>17</sonar.java.source>
	</properties>

	
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.seleniumhq.selenium</groupId>
				<artifactId>selenium-java</artifactId>
				<version>${selenium.version}</version>
			</dependency>
			<dependency>
				<groupId>org.testng</groupId>
				<artifactId>testng</artifactId>
				<version>${testng.version}</version>
			</dependency>
			<dependency>
				<groupId>io.rest-assured</groupId>
				<artifactId>rest-assured</artifactId>
				<version>${rest.assured.version}</version>
			</dependency>
			<dependency>
				<groupId>io.rest-assured</groupId>
				<artifactId>json-schema-validator</artifactId>
				<version>${rest.assured.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.poi</groupId>
				<artifactId>poi</artifactId>
				<version>${apache.poi.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.poi</groupId>
				<artifactId>poi-ooxml</artifactId>
				<version>${apache.poi.version}</version>
			</dependency>
			<dependency>
				<groupId>com.aventstack</groupId>
				<artifactId>extentreports</artifactId>
				<version>${extent.report.version}</version>
			</dependency>
			<dependency>
				<groupId>io.cucumber</groupId>
				<artifactId>cucumber-java</artifactId>
				<version>${cucumber.jvm.version}</version>
			</dependency>
			<dependency>
				<groupId>io.cucumber</groupId>
				<artifactId>cucumber-testng</artifactId>
				<version>${cucumber.jvm.testng.version}</version>
			</dependency>
			<dependency>
				<groupId>io.cucumber</groupId>
				<artifactId>cucumber-picocontainer</artifactId>
				<version>${cucumber.picocontainer.version}</version>
			</dependency>
			<dependency>
				<groupId>commons-io</groupId>
				<artifactId>commons-io</artifactId>
				<version>${commons.io.version}</version>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-databind</artifactId>
				<version>${jackson.databind.version}</version>
			</dependency>
			<dependency>
				<groupId>org.aeonbits.owner</groupId>
				<artifactId>owner</artifactId>
				<version>${owner.version}</version>
			</dependency>
			<dependency>
				<groupId>com.github.javafaker</groupId>
				<artifactId>javafaker</artifactId>
				<version>${faker.version}</version>
			</dependency>
			<dependency>
				<groupId>com.mysql</groupId>
				<artifactId>mysql-connector-j</artifactId>
				<version>${mysql.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-api</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>ch.qos.logback</groupId>
				<artifactId>logback-classic</artifactId>
				<version>${logback.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>jul-to-slf4j</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>net.logstash.logback</groupId>
				<artifactId>logstash-logback-encoder</artifactId>
				<version>${logstash.encoder.version}</version>
			</dependency>
			<dependency>
				<groupId>org.wiremock</groupId>
				<artifactId>wiremock-standalone</artifactId>
				<version>${wiremock.version}</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>com.tngtech.archunit</groupId>
				<artifactId>archunit</artifactId>
				<version>${archunit.version}</version>
				<scope>test</scope>
			</dependency>

			<!-- Version-override pins only — no module declares these as a direct
			     dependency; they take effect if/when a module's own dependency
			     graph transitively pulls in an older copy. -->
			<dependency>
				<groupId>com.google.code.gson</groupId>
				<artifactId>gson</artifactId>
				<version>${gson.version}</version>
				<!-- overrides extentreports' transitive gson:2.10.1 -->
			</dependency>
			<dependency>
				<groupId>org.yaml</groupId>
				<artifactId>snakeyaml</artifactId>
				<version>${snakeyaml.version}</version>
				<!-- overrides javafaker's transitive snakeyaml:android:1.23 -->
			</dependency>
			<dependency>
				<groupId>com.google.protobuf</groupId>
				<artifactId>protobuf-java</artifactId>
				<version>${protobuf.version}</version>
				<!-- overrides mysql-connector-j's transitive protobuf-java:4.31.1 -->
			</dependency>
			<dependency>
				<groupId>org.jspecify</groupId>
				<artifactId>jspecify</artifactId>
				<version>${jspecify.version}</version>
				<!-- matches selenium-api's own transitive need at the same version -->
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.3.0</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<!-- Maven Surefire Plugin for TestNG -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.0.0-M7</version>
				<configuration>
					<suiteXmlFiles>
						<suiteXmlFile>${testng.suite.file}</suiteXmlFile>
					</suiteXmlFiles>
					<systemPropertyVariables>
						<testng.suite.file>${testng.suite.file}</testng.suite.file>
					</systemPropertyVariables>
				</configuration>
			</plugin>

			<!-- JaCoCo — generates coverage data consumed by SonarQube, and enforces a
			     coverage floor via the "check" execution below. -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.12</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals><goal>prepare-agent</goal></goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals><goal>report</goal></goals>
					</execution>
					<execution>
						<id>jacoco-check</id>
						<phase>verify</phase>
						<goals><goal>check</goal></goals>
						<configuration>
							<!-- Coverage percentage is observability, not a build gate: it's a
							     separate, pre-existing concern from "do the unit tests pass"
							     (the thing a plain `mvn install`/`mvn test` is meant to enforce
							     — see testng.suite.file overrides throughout this reactor).
							     Some modules (e.g. autoframex-performance) don't meet this floor
							     yet; haltOnFailure=false means the check still runs and still
							     WARNs in the log, it just doesn't fail an otherwise-green build.
							     Override per-invocation with -Djacoco.haltOnFailure=true if you
							     want coverage to be a hard gate for a specific run. -->
							<haltOnFailure>${jacoco.haltOnFailure}</haltOnFailure>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.20</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- SonarQube static analysis — run with: mvn sonar:sonar -Dsonar.host.url=... -Dsonar.login=... -->
			<plugin>
				<groupId>org.sonarsource.scanner.maven</groupId>
				<artifactId>sonar-maven-plugin</artifactId>
				<version>3.11.0.3922</version>
			</plugin>

			<!-- Checkstyle — report-only (failOnViolation off); see docs/CODING_STANDARDS.md. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<version>3.5.0</version>
				<configuration>
					<configLocation>google_checks.xml</configLocation>
					<failOnViolation>false</failOnViolation>
					<violationSeverity>warning</violationSeverity>
				</configuration>
				<executions>
					<execution>
						<id>checkstyle-check</id>
						<phase>verify</phase>
						<goals><goal>check</goal></goals>
					</execution>
				</executions>
			</plugin>

			<!-- PMD — same report-only posture as Checkstyle above, same reason. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-pmd-plugin</artifactId>
				<version>3.26.0</version>
				<configuration>
					<failOnViolation>false</failOnViolation>
					<printFailingErrors>true</printFailingErrors>
				</configuration>
				<executions>
					<execution>
						<id>pmd-check</id>
						<phase>verify</phase>
						<goals><goal>check</goal></goals>
					</execution>
				</executions>
			</plugin>

			<!-- SpotBugs — static bug-pattern detection, same report-only posture. -->
			<plugin>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-maven-plugin</artifactId>
				<version>4.8.6.6</version>
				<configuration>
					<failOnError>false</failOnError>
				</configuration>
				<executions>
					<execution>
						<id>spotbugs-check</id>
						<phase>verify</phase>
						<goals><goal>check</goal></goals>
					</execution>
				</executions>
			</plugin>

			<!-- OWASP Dependency-Check — see docs/TECHNICAL_DEBT_REGISTER.md TD-11.
			     Deliberately NOT bound to a lifecycle phase (NVD feed download is
			     slow/rate-limited) — run explicitly with `mvn dependency-check:check`
			     or via the scheduled dependency-check.yml workflow. -->
			<plugin>
				<groupId>org.owasp</groupId>
				<artifactId>dependency-check-maven</artifactId>
				<version>10.0.4</version>
				<configuration>
					<failBuildOnCVSS>9</failBuildOnCVSS>
					<formats>
						<format>HTML</format>
						<format>JSON</format>
					</formats>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
