# ============================================================
# LeafTaps Project Configuration
# ============================================================
# This file is loaded by LeafTapConfiguration (Owner MERGE policy).
# Framework defaults (browser, waits, pool) come from frameworkConfig.properties.
# ============================================================

# ------------------------------------------------------------
# Application URLs  (referenced by environment parameter in TestNG XML)
# ------------------------------------------------------------
# dev  → <parameter name="environment" value="dev"/>
# qa   → <parameter name="environment" value="qa"/>
# prod → <parameter name="environment" value="prod"/>
leaftaps.dev.url=http://leaftaps.com/opentaps/control/main
leaftaps.qa.url=http://leaftaps.com/opentaps/control/main
leaftaps.prod.url=http://leaftaps.com/opentaps/control/main

# ------------------------------------------------------------
# Login credentials
# ------------------------------------------------------------
leaftaps.userName=Demosalesmanager
leaftaps.password=crmsfa

# ------------------------------------------------------------
# Database
# ------------------------------------------------------------
leaftaps.db.url=jdbc:mps
leaftaps.db.userName=sox
leaftaps.db.password=La32
leaftaps.db.query=select PARTY_ID from opentaps.PERSON order by RAND() limit 1
